DATABASE_URL="file:./dev.db"

# Auth (generate a secret with: openssl rand -base64 32)
AUTH_SECRET=""
ADMIN_EMAIL="admin@nexoraconsulting.com"
ADMIN_PASSWORD="changeme123"

# Google OAuth (Drive + Gmail) - obtain from https://console.cloud.google.com/apis/credentials
# Leave blank to keep Documents/Email pages in "not configured" mode.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI="http://localhost:3000/api/google/callback"
